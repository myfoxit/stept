// src/utils/formulaEvaluator.ts
// -----------------------------------------------------------------------------
//  Preserve the old import path so nothing else breaks.
//  We simply re-export from the new engine.
// -----------------------------------------------------------------------------
export { evaluateFormula, FormulaError } from './formulaEngine';
