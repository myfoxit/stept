# pytest.ini
[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
# Exclude experimental feature tests (run with: pytest tests/experimental/ -v)
collect_ignore_glob = ["experimental/*"]
